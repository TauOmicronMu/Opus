(* Simple program written in Opus *)

let void main = {

    (*Increment all elements of a list*)	
    let List<int> increment_list xs = {
        for i in xs {
	    xs[i]++
        } 

    (* Print hello world *)
    let void hello_world = {
	print("Hello World")
    }
    
    (* Creating an example class *)
    class example {
        let int n
       
	let __init__(int n) = {
	    self.n = n
        }
	
	let int getn = {
	    self.n
	}

	let void setn(int n) = {
	    self.n = n
	}
    }
} 
